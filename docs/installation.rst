============
Installation
============

At the command line::

    $ easy_install pleiades.serializations

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pleiades.serializations
    $ pip install pleiades.serializations